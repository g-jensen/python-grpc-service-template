[project]
name = "python-grpc-service-template"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "grpcio-tools>=1.83.1",
    "typer>=0.27.2",
]

[dependency-groups]
test = [
    "pytest>=9.1.1",
    "pytest-mock>=3.15.1",
    "pytest-grpc>=0.8.0",
]